import { shopMeta } from '@/lib/metadata'

export const metadata = shopMeta.pendingApproval

export default function PendingApprovalLayout({ children }: { children: React.ReactNode }) {
  return children
}
